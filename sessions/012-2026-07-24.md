# Session 012 — 2026-07-24

## Goal / Context
The 49-episode reviewed manifest (`all_batches_reviewed_manifest _final.json`) has been
exported from the portal. Convert it back to the standard training `manifest.json` format
using `scripts/convert_reviewed_manifest.py`. Final output name: `manifest_reviewed.json`.
This kicks off the turnkey full-run sequence staged in session 011.

## Key Decisions
- Converted 49-ep reviewed manifest → standard format. Since it spans BOTH batch folders,
  used the converter's global mode and wrote output to `data/processed/manifest_reviewed.json`
  (NOT inside a single batch folder). dataset_builder's default input still points at
  `Batch1_EP23/manifest_reviewed.json`, so the build step must pass the root path explicitly.

## Done This Session
- `convert_reviewed_manifest.py` on `all_batches_reviewed_manifest _final.json`:
  **2260/2260 clips matched, 49/49 episodes, 0 missing**, resolved across Batch1_EP23 +
  Batch2_EP24_EP50. Output: `data/processed/manifest_reviewed.json`.
- Verified: 2260 entries, correct keys (audio_path/transcript/duration/language),
  **11.69 hrs** total, 0 audio files missing on disk.

## Nastaliq fix — SKIPPED (team handled in review)
- Team confirmed English-in-Nastaliq was cleaned during portal review.
- Verification scan of converted manifest vs the ~150 known mis-spellings: only 1 pattern
  (`ال بی`) hit, both occurrences FALSE POSITIVES — the match straddles the word boundary in
  `خدوخال بیان` ("khad-o-khaal bayan"). Running the fix would have corrupted these.
- Decision: do NOT run `fix_english_in_nastaliq.py` on this manifest.

## Smoke-test model backup
- Backing up smoke-test LoRA model from Modal volume → local
  `C:\Users\Mohammad Touseef\Downloads\Whisper Smoke Test Trained Model`.
- Downloaded ✅ COMPLETE + integrity-verified: `whisper-urdu-lora-adapter` (60MB, 10 files)
  + `whisper-urdu-final` (5.75GB fp32, 12 files, 1259 tensors, both shards INTACT — data
  offsets match file sizes). Skipped checkpoints (only needed to resume training).
- NOTE: merged model is fp32 (~6GB), not ~3GB. First bg download STALLED (shard1 stuck at
  0B) — had to kill + restart; verify shard growth, don't trust the ✓ line alone.
- LoRA context: adapter (60MB) is the only trained artifact; base large-v3 is
  re-downloadable from HF. `whisper-urdu-final` = adapter merged into base (standalone).
- ⚠️ GOTCHA: `modal volume get <dir>` collapses to ONE file unless local dest dir
  pre-exists. Fix: New-Item -Directory first, then get. Saved to memory.

## domain_terms.json expanded (2026-07-25)
- Mined 49-ep manifest: added 35 izafat spiritual_terms (freq>=4) + 4 diacritized durood
  fragments. Now 60 spiritual_terms + 11 arabic_phrases.
- FINDING: original 7 undiacritized arabic_phrases match 0 clips (reviewers wrote Arabic
  fully diacritized) — kept only for initial_prompt biasing.
- spiritual_term bucket coverage: 153 → 687 clips (6.8% → 30.4%).
- Auto eval-pick shifted (richness ranking changed): now 7 eps / 246 clips (10.9%),
  spiritual-dense: EP5,EP6,EP34,EP41,EP43,EP44,EP47. Train = 42 eps / 2014 clips.
- DECISION PENDING: pin these 7 as --eval-episodes (stable across re-runs) vs leave auto.

## Faulty chunks removed (2026-07-25)
- Removed 3 faulty clips (audio deleted + entry removed from manifest_reviewed.json ONLY,
  per user): EP3 idx038, EP38 idx010, EP38 idx011.
- manifest_reviewed.json: 2260 → 2257 clips, 11.69 → 11.67 hrs.
- Source batch manifests + reviewed export left intact (user choice).
- ⚠️ CAVEAT: re-running convert_reviewed_manifest.py will regenerate these 3 and hard-fail
  on missing audio. Re-do removal after any re-convert, or clean source manifests then.
- Auto eval-pick / max_steps will recompute off 2257 clips now.

## Dataset built + config set (2026-07-25)
- Built HF dataset with PINNED eval episodes (EP5,6,34,41,43,44,47).
  DatasetDict {train:2011, eval:246}. Wrote eval_buckets.json (246 entries).
  Buckets — train/eval: nastaliq_only 1695/194, code_switch 316/52, spiritual_term 558/129.
  (52% of eval clips carry a spiritual term — eval exercises the domain goal.)
- Cleaned a stale orphan train arrow (data-00000-of-00001.arrow, Jul-6 smoke-test build,
  298MB, not referenced by state.json) — save_to_disk doesn't wipe target dir.
- training_config.yaml: max_steps 120→567 (9 epochs @ 63 steps/epoch), warmup 20→57,
  eval_steps/save_steps 30→63 (once per epoch). Effective batch=32 (8×4), Tier-2 LoRA.

## Modal volume cleaned for fresh run (2026-07-25)
- Confirmed train() is a FRESH run: loads base whisper-large-v3 + NEW LoRA (no resume,
  no checkpoint load). Saves to same /model paths → WOULD overwrite smoke-test model
  (now backed up locally). Tier-1→Tier-2 shape change means resume wasn't possible anyway.
- Deleted from volume: /model (old adapter+final), /checkpoints (60/90/120), /logs (stale).
- Also earlier deleted /processed/dataset (old smoke-test dataset).
- Volume now: /processed (empty) + /config (old training_config.yaml, to be overwritten).

## Upload to Modal (2026-07-25)
- Pre-flight verified: dataset 2011/246 + eval_buckets aligned, config max_steps=567 Tier-2,
  domain_terms 60/11/10. GPU decision: STAY on A10G (LoRA+grad-ckpt ~10-14GB, fits 24GB;
  Tier-2 doesn't raise peak VRAM materially). OOM fallback: batch4 + grad_accum8 (eff still 32).
- ✅ config/ → /config (4 files, no nesting).
- ✅ dataset 1.3GB → /processed/dataset (train 3 shards + eval 1 shard + dataset_dict +
  eval_buckets.json; verified on volume, no nesting/orphans).
- NEXT per CLAUDE.md: re-baseline `evaluate --which base` on NEW 246-clip eval set BEFORE train.

## Baseline eval DONE (2026-07-25)
- Base large-v3 on new 246-clip eval set: overall norm WER 18.57% (raw 23.87%).
  Per-bucket: nastaliq_only 18.05%, code_switch 20.46%, spiritual_term 20.29%.
- Code-switch + spiritual ~2pts worse than plain nastaliq → the fine-tune's target areas.
- Recorded in FULL_WHISPER_TRAINING_RUN.md. LoRA r=32 kept (change breadth via Tier-2, not
  rank; r=64/alpha128 is the next lever IF spiritual bucket underperforms).
- NEXT: modal run train.

## Training attempt 1 FAILED — local network drop (2026-07-25)
- Started 01:54:02. Healthy: Tier-2 LoRA 57.6M/3.6%, NO OOM at batch8, loss 0.49→0.43→0.35,
  epoch-1 eval_wer 21.17 raw (vs base raw 23.87 — already improving). ~10s/step.
- Died at step 88/567 (~27min): LOCAL machine DNS drop (getaddrinfo failed) → heartbeat
  failed → non-detached `modal run` client exited → Modal tore down the app (now stopped).
- FIX: relaunch with `modal run --detach` (survives local disconnects). checkpoint-63 survived
  on volume but train() has no resume; will delete it + fresh detached run.
- VRAM concern RESOLVED (no OOM at batch8 Tier-2).

## Training attempt 2 SUCCEEDED (--detach) — 2026-07-25
- Detached run completed all 567 steps (3h14m) server-side despite local client
  disconnecting at ~step 250. --detach validated.
- Best = epoch 6 (checkpoint-378), RAW eval_wer 15.71% (base raw 23.87% → ~34% rel. reduction).
  Curve: 21.16→17.69→16.42→16.05→15.78→15.71→15.98→15.90→15.89 (7-9 mild overfit).
- Model saved to /model/whisper-urdu-{lora-adapter,final}; checkpoints 378/504/567 kept.
- NEXT: `modal run --detach modal_app.py::evaluate` for NORMALIZED + per-bucket base-vs-FT
  (headline metric; trainer numbers are raw). Then download + push to HF Hub.

## FINAL EVAL DONE — big win (2026-07-25)
- Normalized WER: base 18.57% → fine-tuned 10.50% (+8.07 pts). BEATS <13% target, near <10%.
- Per-bucket Δ: nastaliq 18.05→9.89 (+8.16), code_switch 20.46→12.70 (+7.76),
  spiritual_term 20.29→9.38 (+10.91 ⭐ — worst→best, validates Tier-2 fc1/fc2 for domain terms).
- vs smoke test which was only +0.75 pts. Strategy fully validated.
- Results in FULL_WHISPER_TRAINING_RUN.md + /logs/eval_results.json on volume.
- REMAINING: download /model/whisper-urdu-final locally + push to HF Hub (volumes not permanent!).

## Qualitative compare done (2026-07-25)
- Ran compare_transcribe.py (base vs FT) on EP19+EP2 → full_audio_samples/compare_transcripts/
  Full Fine Tuned/. Changed script out_dir to "Full Fine Tuned" subfolder.
- FT wins: diacritized Arabic durood, English-in-Latin (چیلنج→challenge), word fixes
  (بیوی→بی بی مریم, ایسا→عیسیٰ), less base looping, proper Urdu diacritics.
- Tradeoff: FT occasionally over-diacritizes/garbles Arabic + run-on segmentation.
- Noted in FULL_WHISPER_TRAINING_RUN.md.
- 3rd clip (Yahoodi, longer/harder): base REPETITION COLLAPSE (یہ×100+) vs FT clean —
  biggest win yet; English→Latin consistent; base موسیقی→FT correct durood. Added to doc.
- Added a "What is WER" explainer to FULL_WHISPER_TRAINING_RUN.md (S+D+I/N, examples, raw vs norm).

## HF Hub push DONE (2026-07-25)
- New scripts/push_to_hub.py: Modal fn uses huggingface_hub.upload_folder from
  /model/whisper-urdu-final + "huggingface-secret" (HF_TOKEN). Ran --detached.
- Pushed 12 files (6.17GB) to mohammad-toseef059/whisper-large-v3-urdu (PRIVATE). Done.
- Model now durably stored off the Modal volume. Share via repo collaborators / org.
- Untracked to commit later: scripts/push_to_hub.py, scripts/push_readme.py, MODEL_CARD.md,
  FULL_WHISPER_TRAINING_RUN.md, edited scripts/compare_transcribe.py, Full Fine Tuned transcripts.
- README/model card pushed to HF repo via scripts/push_readme.py (add_local_file MODEL_CARD.md
  → README.md, uses huggingface-secret). Repo now has model card + 12 model files.

## Open Items / TODOs
- ✅ DONE this session: convert, expand domain_terms, remove faulty chunks, build dataset,
  set max_steps (567), upload, re-baseline, train (--detach), per-bucket eval, HF push + README.
- STILL PENDING (future work, not blocking):
  - Manual listen+read spot-check on EP24–50 chunks (never done).
  - CLAUDE.md Phase 5: WhisperX timestamp validation on sample clips.
  - CLAUDE.md Phase 6 (production): convert HF model → faster-whisper (ct2 int8) + WhisperX,
    batch-transcribe the 4,000 videos.
  - Optional accuracy push toward <10%: wire in initial_prompt.txt + corrections.json at inference.
  - Optional: r=64/α=128 experiment IF a future eval shows the spiritual bucket needs more
    capacity (current 9.38% is already strong, so low priority).
  - Housekeeping: add data artifacts (*.mp3/*.xlsx/*.docx) to .gitignore to avoid pull conflicts.

## Next Steps
1. **Production pipeline (Phase 6):** convert `mohammad-toseef059/whisper-large-v3-urdu` to
   faster-whisper (`ct2-transformers-converter --quantization int8`) + WhisperX for timestamps,
   then batch-transcribe the 4,000 videos.
2. **Timestamp validation (Phase 5):** run WhisperX on a few clips, confirm alignment quality.
3. Share the private HF repo with teammates (repo Settings → Collaborators) as needed.

## Summary
Executed the full 49-episode LoRA fine-tuning run end-to-end. Normalized WER improved
**18.57% → 10.50%** (+8.07 pts), beating the CLAUDE.md <13% target; the **spiritual_term bucket
went from worst (20.29%) to best (9.38%), +10.91 pts**, validating the Tier-2 MLP LoRA targets
(fc1/fc2) added for domain-term learning. Qualitative compares on 3 full episodes confirmed the
gains (diacritized Arabic, English-in-Latin, real word fixes, and elimination of a catastrophic
base-model repetition loop). The merged model + a model card were pushed to a **private HF repo**
(`mohammad-toseef059/whisper-large-v3-urdu`); the smoke-test model was backed up locally first.
Key recurring lessons (saved to memory): use `modal run --detach` for long runs (a local network
drop killed attempt 1 at step 88), and pre-create the local dir for `modal volume get`.
