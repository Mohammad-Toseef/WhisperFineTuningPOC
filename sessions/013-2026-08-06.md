# Session 013 — 2026-08-06

## Goal / Context
Consolidate everything related to Whisper training into a single repo (`WhisperFineTuningPOC`).
Today: identify what must move over from the sibling `SRTTimeStampPOC` project — which is
effectively the *upstream* half of the pipeline (YouTube download → intro-music trim →
forced alignment → timestamped SRT) whose output this repo consumes as training data.

## Findings — repo relationship
- **SRTTimeStampPOC** = data acquisition + labeling front end.
  **WhisperFineTuningPOC** = training back end. Two halves of one pipeline, split across repos.
- The **corpus is already mirrored**: `samples/28JulyBatch/` (EP1–EP23) and
  `samples/Batch2_EP24_EP50/` hold the srt+mp3 pairs.
- ✅ Verified the `samples/*.mp3` files are **byte-identical to SRTTimeStampPOC's
  `music_cleaned_audio/`** (spot-checked EP1, EP10, EP19), i.e. the music-TRIMMED audio, not
  the raw downloads. So SRT timings and training audio share one timeline — **no offset bug.**
- Gap found: **EP39__ojnZZ8muws** exists in SRT `timestamped_srts/Batch2` (+ cleaned audio)
  but is MISSING from `samples/Batch2_EP24_EP50/`.
- What is genuinely absent from this repo is the **tooling** that produces the corpus.

## Copy list — must move (~60 KB, the upstream pipeline)
| Source file (SRTTimeStampPOC) | Why |
|---|---|
| `download_playlist_audio.py` | Step 1 acquisition; makes the 1.2 GB `audio/` reproducible instead of a permanent blob. |
| `clean_intro_music.py` | ffmpeg trim primitive — produces exactly the audio the dataset is built from. |
| `batch_clean_intro_music.py` | Batch driver over the skip-start sheet. |
| `align_to_srt.py` | **Core value.** whisperx forced alignment: verified text → timestamped SRT. `src/srt_audio_prep.py` consumes its output. |
| `modal_align.py` | GPU runner for the above; natural sibling of `modal_app.py`. |
| `episode_skip_start_v2.xlsx` | Hard input to the batch trim. Hand-verified, NOT derivable — without it the cleaned audio is unreproducible. |
| `Whisper_Second_Round_Training_list.xlsx` | Round-2 episode plan (edited 2026-08-05). Pure training artifact sitting in the wrong repo. |

## Copy list — should move (provenance)
- `raw_transcripts/` (50 files, 0.8 MB) — human-verified ground-truth text; the actual
  **label source** (SRTs = this text + timings). Small and irreplaceable.
- EP39 srt + cleaned mp3 — close the corpus gap noted above.
- `sessions/003…007` — the alignment decision log (why fuzzy-match anchors, why trim before align).

## Leave behind in SRTTimeStampPOC
- `audio/` (1.2 GB raw) and `models/faster-whisper-small` — regenerable by the copied scripts.
- `music_cleaned_audio/` (1 GB) — already mirrored into `samples/` except EP39.
- `hadees_snips.py` + `hadees_snips/` — different product (translation-portal frame extraction),
  not training. Stays.
- `*.log`, `EP3.*` scratch files, `.venv/`, `__pycache__/`, superseded `episode_skip_start*` versions.

## Key Decisions
- ✅ Layout CONFIRMED (user): `align_to_srt.py` + trim scripts → `src/srt_pipeline/` subpackage;
  `download_playlist_audio.py` → `scripts/`; `modal_align.py` → repo root beside `modal_app.py`.
  Matches the existing split where `src/` = pipeline stages, `scripts/` = one-off CLIs.
- **NO `__init__.py`** — the repo has none anywhere and uses flat sibling imports
  (`from srt_audio_prep import ...`, `from data_prep import ...`). Kept that convention inside
  `srt_pipeline/`, so `batch_clean_intro_music.py`'s `from clean_intro_music import ...`
  needed no edit. Scripts run from inside their own directory, as the existing ones do.
- ✅ Scope CONFIRMED (user): copy **Group 1 + Group 2 only**. Groups 3–5 (raw_transcripts,
  EP39 gap, SRT session logs) deferred — still listed above when wanted.

## Done This Session — copy executed (7 files, 59 KB)
All 7 verified byte-identical to source (size match src vs dest) and all 5 `.py` compile clean.

| Source (SRTTimeStampPOC) | → Destination |
|---|---|
| `align_to_srt.py` (9,067 B) | `src/srt_pipeline/align_to_srt.py` |
| `clean_intro_music.py` (2,114 B) | `src/srt_pipeline/clean_intro_music.py` |
| `batch_clean_intro_music.py` (2,913 B) | `src/srt_pipeline/batch_clean_intro_music.py` |
| `modal_align.py` (3,502 B) | `modal_align.py` (root) |
| `download_playlist_audio.py` (2,836 B) | `scripts/download_playlist_audio.py` |
| `episode_skip_start_v2.xlsx` (7,529 B) | `config/episode_skip_start_v2.xlsx` |
| `Whisper_Second_Round_Training_list.xlsx` (31,037 B) | root |

### Edits made (2, both necessary)
- **`modal_align.py`** — added `sys.path.insert(0, <repo>/src/srt_pipeline)` before the image
  definition. Required: the file sits at root but `align_to_srt` moved into the subpackage, so
  BOTH `add_local_python_source("align_to_srt")` (resolves the module locally at build time)
  and the in-container `import align_to_srt as core` would otherwise fail.
  Verified: `find_spec('align_to_srt')` now resolves to `src/srt_pipeline/align_to_srt.py`.
- **`.gitignore`** — added `!config/episode_skip_start_v2.xlsx` and
  `!Whisper_Second_Round_Training_list.xlsx` under the `*.xlsx` rule, with a comment on why.
  Verified via `git check-ignore -v` + `git status` → both now show as `??`, not ignored.
  (Closes part of session 012's "add data artifacts to .gitignore" housekeeping item.)

## Open Items / TODOs
- ⚠️ **Hardcoded relative paths in the copied scripts point at SRT-repo data that was NOT
  copied.** Left as-is deliberately (no invented paths). Affected defaults:
  - `batch_clean_intro_music.py`: `AUDIO_DIR="audio"`, `TRANSCRIPT_DIR="raw_transcripts"`,
    `OUT_DIR="music_cleaned_audio"`, and `--xlsx` default is the bare filename (now under `config/`).
  - `modal_align.py::batch`: `audio_dir="music_cleaned_audio"`, `text_dir="raw_transcripts"`,
    `out_dir="timestamped_srts"`.
  Decide when re-running upstream: either copy Group 3 (`raw_transcripts/`) + audio, or repoint
  these at `samples/`. Not urgent — nothing in the training path calls them today.
- Document the full end-to-end chain (download → trim → align → chunk → train) in README.
- Group 3–5 copies still available if wanted (see lists above).
- Commit the 6 new files + `.gitignore` change (not done — nothing committed this session).

### Carried forward from session 012 (not blocking today)
- Manual listen+read spot-check on EP24–50 chunks (never done).
- CLAUDE.md Phase 5: WhisperX timestamp validation on sample clips.
- CLAUDE.md Phase 6 (production): convert HF model → faster-whisper (ct2 int8) + WhisperX,
  batch-transcribe the 4,000 videos.
- Optional accuracy push toward <10%: wire `initial_prompt.txt` + `corrections.json` at inference.
- Optional r=64/α=128 experiment IF a future eval shows the spiritual bucket needs capacity.
- Share the private HF repo (`mohammad-toseef059/whisper-large-v3-urdu`) with teammates.

## Next Steps
<!-- populate at session end -->

## Summary
<!-- populate at session end -->
